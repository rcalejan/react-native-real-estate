import { StyleSheet } from 'react-native';

export const fontStyles = StyleSheet.create({
  titleM: {
    fontSize: 18,
    fontWeight: '600',
    lineHeight: 24,
  },
  bodyM: {
    fontSize: 16,
    fontWeight: '400',
    lineHeight: 22,
  },
});
